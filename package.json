{
  "name": "paint-center-erp",
  "version": "1.0.0",
  "description": "Paint Center Sales and Inventory Management System",
  "scripts": {
    "install-all": "npm install --prefix frontend && npm install --prefix backend",
    "build": "npm run install-all && npm run build --prefix frontend && cd backend && npx prisma generate && npx prisma migrate deploy",
    "start": "node backend/index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
